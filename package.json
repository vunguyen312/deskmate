{
  "name": "voice-box",
  "version": "0.1.0",
  "main": "dist/main.js",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit && esbuild src/main.ts --bundle --platform=node --format=cjs --external:electron --outfile=dist/main.js && esbuild src/preload.ts --bundle --platform=node --format=cjs --external:electron --outfile=dist/preload.js && esbuild renderer/src/main.ts --bundle --format=iife --outfile=dist/bundle.js && esbuild renderer/src/settings.ts --bundle --format=iife --outfile=dist/settings-bundle.js",
    "typecheck": "tsc --noEmit",
    "start": "npm run build && electron ."
  },
  "dependencies": {
    "@ricky0123/vad-web": "^0.0.30"
  },
  "devDependencies": {
    "electron": "^43.3.0",
    "esbuild": "^0.28.1",
    "typescript": "^5.6.0",
    "@types/node": "^24.0.0"
  }
}
